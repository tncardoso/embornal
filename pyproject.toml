[project]
name = "embornal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Thiago Cardoso", email = "thiagoncc@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
embornal = "embornal:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "zensical>=0.0.51",
]
